<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4"><%= @education_program.name %></h1>
    <p class="lead"><%= @education_program.description %></p>
    <%= link_to t('common.back'), :back, :class => 'btn btn-outline-secondary' %>
  </div>
</div>